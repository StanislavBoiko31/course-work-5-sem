# Налаштування Email для відправки результатів фотосесії
# Скопіюйте цей файл в .env та заповніть своїми даними

# Backend для email (для розробки можна використати 'django.core.mail.backends.console.EmailBackend')
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend

# SMTP налаштування для Gmail
# Варіант 1: SSL (порт 465) - рекомендовано, якщо порт 587 блокується файрволом
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=465
EMAIL_USE_TLS=False
EMAIL_USE_SSL=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=your-email@gmail.com

# Варіант 2: TLS (порт 587) - альтернатива, якщо SSL не працює
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_USE_SSL=False

# Базовий URL для посилань у email (для production змініть на ваш домен)
BASE_URL=http://localhost:8000

# Примітки:
# - Для Gmail потрібно створити "Пароль додатку" (App Password) в налаштуваннях Google Account
# - Звичайний пароль не працюватиме через двофакторну аутентифікацію
# - Пароль додатку вводиться БЕЗ пробілів
# - Якщо порт 587 блокується файрволом/антивірусом, використовуйте порт 465 з SSL
# - Для тестування встановіть EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

