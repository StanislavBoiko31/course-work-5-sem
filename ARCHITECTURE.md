# Архітектура проекту фотостудії

## Загальний огляд

Проект являє собою **full-stack веб-додаток** для управління фотостудією з розділенням на бекенд (Django REST Framework) та фронтенд (React). Використовується архітектура **REST API** для комунікації між компонентами.

---

## 🎯 Загальна структура

```
new project/
├── backend/          # Django REST API сервер
└── frontend/         # React SPA додаток
```

---

## 🔧 BACKEND (Django REST Framework)

### Технологічний стек:
- **Django 4.2.7** - веб-фреймворк
- **Django REST Framework 3.14.0** - API фреймворк
- **Django Simple JWT** - аутентифікація через JWT токени
- **Django CORS Headers** - обробка CORS для фронтенду
- **Pillow** - обробка зображень
- **python-decouple** - управління змінними оточення
- **django-filter** - фільтрація даних

### Архітектура бекенду:

#### 1. **Структура проекту (Django Apps)**

Проект організований за принципом **Django Apps** - кожна функціональна область є окремим додатком:

```
backend/
├── studio/              # Головний проект (налаштування)
│   ├── settings.py      # Конфігурація проекту
│   ├── urls.py          # Головний роутинг
│   ├── wsgi.py          # WSGI конфігурація
│   └── asgi.py          # ASGI конфігурація
│
├── users/               # Управління користувачами
│   ├── models.py        # Модель User (кастомна)
│   ├── serializers.py   # Серіалізатори для API
│   ├── views.py         # API endpoints для користувачів
│   └── urls.py          # URL маршрути
│
├── photographers/       # Управління фотографами
│   ├── models.py        # Модель Photographer
│   ├── serializers.py   # Серіалізатори
│   ├── views.py         # ViewSets та API views
│   └── urls.py          # URL маршрути
│
├── services/            # Управління послугами
│   ├── models.py        # Service, AdditionalService
│   ├── serializers.py   # Серіалізатори
│   ├── views.py         # ViewSets
│   └── urls.py          # URL маршрути
│
├── portfolio/           # Портфоліо та контент головної сторінки
│   ├── models.py        # Portfolio, HomePageContent
│   ├── serializers.py   # Серіалізатори
│   ├── views.py         # ViewSets та API views
│   └── urls.py          # URL маршрути
│
└── bookings/            # Управління бронюваннями
    ├── models.py        # Booking
    ├── serializers.py   # Серіалізатори
    ├── views.py         # ViewSets та кастомні API views
    └── urls.py          # URL маршрути
```

#### 2. **Моделі даних (Database Schema)**

**User** (users/models.py):
- Кастомна модель користувача (AbstractBaseUser)
- Поля: email, first_name, last_name, role, personal_discount, profile_image
- Ролі: user, photographer, admin

**Photographer** (photographers/models.py):
- OneToOne з User
- Поля: bio, phone, photo, services (ManyToMany), work_start, work_end, work_days

**Service** (services/models.py):
- Основні послуги: name, description, price, image, duration

**AdditionalService** (services/models.py):
- Додаткові послуги: name, description, price

**Booking** (bookings/models.py):
- Бронювання: user (опціонально), photographer, service, date, start_time, end_time, status
- Для гостей: guest_first_name, guest_last_name, guest_email
- Результати: result_photos (JSONField), result_videos (JSONField)
- Додаткові послуги: additional_services (ManyToMany)
- Ціна: price (з урахуванням знижки)

**Portfolio** (portfolio/models.py):
- Портфоліо: photographer, image, description, service

**HomePageContent** (portfolio/models.py):
- Singleton модель для контенту головної сторінки
- Поля: title, description, contact_emails (JSONField), contact_phones (JSONField), contact_addresses (JSONField), is_active

#### 3. **API Architecture**

**REST API Endpoints:**

```
/api/token/                    # Отримання JWT токену
/api/token/refresh/            # Оновлення токену
/api/register/                 # Реєстрація користувача

/api/auth/users/myprofile/     # Профіль користувача

/api/services/                 # CRUD для послуг
/api/additional-services/      # CRUD для додаткових послуг

/api/photographers/            # Список фотографів
/api/photographers/{id}/        # Деталі фотографа

/api/portfolio/                # Список портфоліо
/api/portfolio/homepage-content/  # Контент головної сторінки

/api/bookings/                 # CRUD для бронювань
/api/bookings/available_dates/ # Доступні дати для бронювання
/api/bookings/{id}/upload_results/  # Завантаження результатів
/api/bookings/{id}/send_results_email/  # Відправка email з результатами
```

**Архітектурні патерни:**
- **ViewSets** (ModelViewSet) - для стандартного CRUD
- **APIView** - для кастомної логіки (завантаження файлів, email)
- **Serializers** - для валідації та серіалізації даних
- **Permissions** - для контролю доступу (IsAuthenticated, AllowAny, кастомні)

#### 4. **Аутентифікація та авторизація**

- **JWT (JSON Web Tokens)** - для аутентифікації
- **Simple JWT** - реалізація JWT для Django
- **Ролі користувачів:**
  - `user` - звичайний клієнт
  - `photographer` - фотограф (майстер)
  - `admin` - адміністратор

#### 5. **Файлове сховище**

- **MEDIA_ROOT** - локальне зберігання файлів
- Структура:
  ```
  media/
  ├── booking_results/    # Результати фотосесій
  ├── photographers/      # Фото фотографів
  ├── portfolio/          # Портфоліо
  ├── profiles/          # Фото профілів
  └── services/          # Фото послуг
  ```

#### 6. **Email функціональність**

- **SMTP** для відправки email
- Налаштування через `.env` файл
- Відправка результатів фотосесії незареєстрованим користувачам
- Генерація токенів доступу для результатів

#### 7. **Бізнес-логіка**

**Система знижок:**
- Персональна знижка для зареєстрованих користувачів
- Збільшення на 0.5% за кожне завершене бронювання
- Максимальна знижка: 10%
- Застосування до загальної суми (послуга + додаткові послуги)

**Робочий графік фотографів:**
- Визначення доступних дат та слотів
- Перевірка накладання бронювань
- Фільтрація по робочих днях

**Статуси бронювань:**
- "Очікує підтвердження" → "Підтверджено адміністратором" → "Зроблено" → "Завершено"
- Контроль переходів між статусами залежно від ролі

---

## 🎨 FRONTEND (React)

### Технологічний стек:
- **React 18.2.0** - UI бібліотека
- **React Router DOM 6.20.1** - маршрутизація
- **Axios 1.6.2** - HTTP клієнт
- **Bootstrap 5.3.2** - CSS фреймворк
- **React Bootstrap 2.9.0** - React компоненти Bootstrap
- **react-datepicker 8.9.0** - вибір дат

### Архітектура фронтенду:

#### 1. **Структура проекту**

```
frontend/src/
├── api/
│   └── studioApi.js          # API функції
│
├── components/                # Переіспользувані компоненти
│   ├── BookingForm.js        # Форма бронювання
│   ├── Header.js             # Навігаційна панель
│   ├── OrdersTable.js        # Таблиця замовлень
│   └── ZoomableImage.js      # Компонент збільшення зображень
│
├── pages/                    # Сторінки додатку
│   ├── PortfolioPage.js      # Головна сторінка (портфоліо)
│   ├── PhotographersPage.js  # Список фотографів
│   ├── ServicesPage.js       # Список послуг
│   ├── BookingPage.js        # Створення бронювання
│   ├── ProfilePage.js        # Особистий кабінет клієнта
│   ├── PhotographerProfilePage.js  # Кабінет фотографа
│   ├── LoginPage.js          # Вхід
│   ├── RegistrationPage.js   # Реєстрація
│   ├── ServiceDetailPage.js  # Деталі послуги
│   ├── PhotographerPortfolioPage.js  # Портфоліо фотографа
│   └── Admin*.js             # Адмін-панелі
│
├── App.js                    # Головний компонент (роутинг)
├── App.css                   # Глобальні стилі
├── axiosInstance.js          # Налаштування Axios
└── index.js                  # Точка входу
```

#### 2. **Роутинг (React Router)**

**Публічні маршрути:**
- `/` - Головна сторінка (портфоліо)
- `/photographers` - Список фотографів
- `/photographers/:id/portfolio` - Портфоліо фотографа
- `/services` - Список послуг
- `/services/:id` - Деталі послуги
- `/booking` - Створення бронювання
- `/login` - Вхід
- `/register` - Реєстрація

**Приватні маршрути (потрібна аутентифікація):**
- `/profile` - Особистий кабінет клієнта
- `/photographer/profile` - Кабінет фотографа
- `/admin/*` - Адмін-панелі

#### 3. **State Management**

- **Local State** (useState) - для локального стану компонентів
- **Context API** - для глобального стану користувача (UserContext)
- **localStorage** - для зберігання JWT токенів
- **Axios Interceptors** - для автоматичного додавання токенів до запитів

#### 4. **Комунікація з бекендом**

**Axios Instance** (`axiosInstance.js`):
- Базовий URL: `http://127.0.0.1:8000/api/`
- Автоматичне додавання JWT токенів
- Обробка помилок аутентифікації

**API функції** (`api/studioApi.js`):
- `getPhotographers()` - отримання списку фотографів
- `getServices()` - отримання списку послуг
- `getPortfolio()` - отримання портфоліо

#### 5. **Компоненти**

**BookingForm:**
- Форма створення бронювання
- Вибір послуги, фотографа, дати, часу
- Вибір додаткових послуг
- Розрахунок ціни з урахуванням знижки
- Блокування недоступних дат

**OrdersTable:**
- Відображення замовлень користувача
- Редагування замовлень
- Завантаження результатів (фото/відео)
- Відображення додаткових послуг

**Header:**
- Навігаційна панель
- Адаптивне меню залежно від ролі користувача
- Посилання на профіль

#### 6. **Сторінки**

**PortfolioPage:**
- Hero-секція з контентом головної сторінки
- Секція контактів
- Фільтроване портфоліо
- Теги фотографів та послуг

**AdminHomePageContentPage:**
- Редагування контенту головної сторінки
- Управління списками email, телефонів, адрес

**PhotographerProfilePage:**
- Список бронювань фотографа
- Завантаження результатів
- Зміна статусів
- Статистика

#### 7. **Стилізація**

- **Bootstrap 5** - для базових стилів
- **Custom CSS** - для специфічних компонентів
- **Inline styles** - для динамічних стилів
- **Responsive design** - адаптивний дизайн

---

## 🔄 Комунікація Frontend ↔ Backend

### Протокол:
- **HTTP/HTTPS** для всіх запитів
- **REST API** - стандартні HTTP методи (GET, POST, PUT, PATCH, DELETE)
- **JSON** - формат обміну даними

### Аутентифікація:
1. Користувач входить через `/api/token/`
2. Отримує `access` та `refresh` токени
3. `access` токен зберігається в `localStorage`
4. Кожен запит містить заголовок: `Authorization: Bearer <token>`
5. При закінченні `access` токену використовується `refresh` токен

### CORS:
- Налаштовано через `django-cors-headers`
- Дозволено запити з `http://localhost:3000`

---

## 📊 База даних

### Тип БД:
- **SQLite** (за замовчуванням для розробки)
- Можна легко перейти на PostgreSQL/MySQL

### Відносини:
- **User** ↔ **Photographer** (OneToOne)
- **Photographer** ↔ **Service** (ManyToMany)
- **Booking** ↔ **User** (ForeignKey, опціонально)
- **Booking** ↔ **Photographer** (ForeignKey)
- **Booking** ↔ **Service** (ForeignKey)
- **Booking** ↔ **AdditionalService** (ManyToMany)
- **Portfolio** ↔ **Photographer** (ForeignKey)
- **Portfolio** ↔ **Service** (ForeignKey)

---

## 🚀 Розгортання

### Backend:
```bash
cd backend
python manage.py migrate
python manage.py runserver
```

### Frontend:
```bash
cd frontend
npm install
npm start
```

### Environment Variables:
- `.env` файл для налаштування email та інших параметрів
- Приклад: `backend/env.example`

---

## 🔐 Безпека

- **JWT токени** для аутентифікації
- **CORS** налаштування
- **Permission classes** для контролю доступу
- **Валідація даних** на рівні серіалізаторів
- **Захищені маршрути** на фронтенді

---

## 📝 Особливості архітектури

1. **Модульність** - кожна функціональна область в окремому Django app
2. **RESTful API** - стандартні REST принципи
3. **Separation of Concerns** - чітке розділення бекенду та фронтенду
4. **Singleton Pattern** - для HomePageContent (завжди один запис)
5. **File Storage** - локальне зберігання медіа файлів
6. **Email Integration** - для доставки результатів
7. **Discount System** - динамічна система знижок
8. **Role-Based Access** - контроль доступу за ролями

---

## 🎯 Майбутні покращення

- Можливе додавання WebSocket для real-time оновлень
- Інтеграція з хмарним сховищем для медіа файлів
- Кешування на рівні API
- Оптимізація запитів до БД
- Тестування (unit tests, integration tests)

