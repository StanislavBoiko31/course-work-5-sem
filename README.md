# 📸 Photo Studio Project

Фотостудія з багатомовною підтримкою (українська/англійська)

## 🚀 Швидкий запуск

### Вимоги
- **Python 3.8+**
- **Node.js 16+**
- **Git**

### Крок 1: Клонування проекту
```bash
git clone [URL_РЕПОЗИТОРІЮ]
cd photo-studio-project
```

### Крок 2: Налаштування Backend (Django)

```bash
# Переходимо в папку backend
cd backend

# Створюємо віртуальне середовище
python -m venv venv

# Активуємо віртуальне середовище
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# Встановлюємо залежності
pip install -r requirements.txt

# Створюємо базу даних SQLite (автоматично)
python manage.py makemigrations
python manage.py migrate

# Створюємо суперкористувача (адміністратора)
python manage.py createsuperuser

# Запускаємо сервер розробки
python manage.py runserver
```

Backend буде доступний за адресою: http://localhost:8000

### Крок 3: Налаштування Frontend (React)

```bash
# Відкриваємо новий термінал і переходимо в папку frontend
cd frontend

# Встановлюємо залежності
npm install

# Запускаємо сервер розробки
npm start
```

Frontend буде доступний за адресою: http://localhost:3000

## 🗄️ База даних

### SQLite (за замовчуванням)
Проект використовує SQLite як базу даних за замовчуванням. Файл бази даних буде створений автоматично в `backend/db.sqlite3`.

### PostgreSQL (рекомендовано для продакшену)
Для використання PostgreSQL:

1. **Встановіть PostgreSQL**
2. **Створіть базу даних:**
   ```sql
   CREATE DATABASE photo_studio;
   CREATE USER photo_user WITH PASSWORD 'your_password';
   GRANT ALL PRIVILEGES ON DATABASE photo_studio TO photo_user;
   ```

3. **Встановіть psycopg2:**
   ```bash
   pip install psycopg2-binary
   ```

4. **Оновіть налаштування в `backend/photo_studio/settings.py`:**
   ```python
   DATABASES = {
       'default': {
           'ENGINE': 'django.db.backends.postgresql',
           'NAME': 'photo_studio',
           'USER': 'photo_user',
           'PASSWORD': 'your_password',
           'HOST': 'localhost',
           'PORT': '5432',
       }
   }
   ```

5. **Виконайте міграції:**
   ```bash
   python manage.py migrate
   ```

### MySQL
Для використання MySQL:

1. **Встановіть MySQL**
2. **Створіть базу даних:**
   ```sql
   CREATE DATABASE photo_studio CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   CREATE USER 'photo_user'@'localhost' IDENTIFIED BY 'your_password';
   GRANT ALL PRIVILEGES ON photo_studio.* TO 'photo_user'@'localhost';
   FLUSH PRIVILEGES;
   ```

3. **Встановіть mysqlclient:**
   ```bash
   pip install mysqlclient
   ```

4. **Оновіть налаштування в `backend/photo_studio/settings.py`:**
   ```python
   DATABASES = {
       'default': {
           'ENGINE': 'django.db.backends.mysql',
           'NAME': 'photo_studio',
           'USER': 'photo_user',
           'PASSWORD': 'your_password',
           'HOST': 'localhost',
           'PORT': '3306',
           'OPTIONS': {
               'charset': 'utf8mb4',
           },
       }
   }
   ```

5. **Виконайте міграції:**
   ```bash
   python manage.py migrate
   ```

## 📁 Структура проекту

```
photo-studio-project/
├── backend/                 # Django backend
│   ├── manage.py
│   ├── requirements.txt
│   ├── photo_studio/       # Основні налаштування Django
│   ├── photographers/      # Додаток фотографів
│   ├── services/          # Додаток послуг
│   ├── bookings/          # Додаток бронювань
│   ├── db.sqlite3         # База даних SQLite (створюється автоматично)
│   └── media/             # Завантажені файли
├── frontend/              # React frontend
│   ├── package.json
│   ├── public/
│   ├── src/
│   │   ├── components/    # React компоненти
│   │   ├── pages/        # Сторінки
│   │   ├── contexts/     # React Context
│   │   ├── locales/      # Файли перекладів
│   │   └── api/          # API клієнт
│   └── build/            # Збірка для продакшену
└── README.md
```

## 🌍 Багатомовність

Проект підтримує українську та англійську мови:
- Перемикач мови в хедері (кнопка "EN"/"УК")
- Автоматичне збереження вибору мови
- Легке додавання нових перекладів

## 👥 Ролі користувачів

### Адміністратор
- Управління фотографами
- Управління послугами
- Перегляд всіх бронювань
- Доступ до адмін-панелі

### Фотограф
- Особистий кабінет
- Управління портфоліо
- Перегляд своїх бронювань
- Обмежений доступ до навігації

### Клієнт/Гість
- Перегляд портфоліо
- Перегляд послуг
- Бронювання фотосесій
- Реєстрація/авторизація

## 🔧 Налаштування для продакшену

### Backend
```bash
# Налаштування змінних середовища
cp .env.example .env
# Відредагуйте .env файл

# Збірка статичних файлів
python manage.py collectstatic

# Запуск з Gunicorn
pip install gunicorn
gunicorn photo_studio.wsgi:application
```

### Frontend
```bash
# Збірка для продакшену
npm run build

# Обслуговування через nginx або інший веб-сервер
```

## 🐛 Розв'язання проблем

### Проблема: "Module not found"
```bash
# Переконайтеся, що віртуальне середовище активоване
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# Перевстановіть залежності
pip install -r requirements.txt
```

### Проблема: "npm ERR!"
```bash
# Очистіть кеш npm
npm cache clean --force

# Видаліть node_modules і перевстановіть
rm -rf node_modules package-lock.json
npm install
```

### Проблема: "Database connection failed"
```bash
# Перевірте, чи запущений Django сервер
python manage.py runserver

# Перевірте налаштування бази даних в settings.py

# Для SQLite перевірте права доступу до файлу db.sqlite3
```

### Проблема: "No module named 'psycopg2'"
```bash
# Встановіть psycopg2 для PostgreSQL
pip install psycopg2-binary
```

### Проблема: "No module named 'mysqlclient'"
```bash
# Встановіть mysqlclient для MySQL
pip install mysqlclient
```

## 📞 Підтримка

Якщо виникли проблеми:
1. Перевірте версії Python (3.8+) та Node.js (16+)
2. Переконайтеся, що всі залежності встановлені
3. Перевірте, чи запущені обидва сервери (backend на порту 8000, frontend на порту 3000)
4. Перевірте налаштування бази даних

## 🚀 Швидкі команди

```bash
# Запуск всього проекту (в різних терміналах)
# Термінал 1:
cd backend && python manage.py runserver

# Термінал 2:
cd frontend && npm start
```

## 📊 Створення тестових даних

Після створення суперкористувача, ви можете:

1. **Увійти в адмін-панель:** http://localhost:8000/admin
2. **Створити фотографів** через адмін-панель
3. **Додати послуги** для кожного фотографа
4. **Завантажити фото** в портфоліо

Готово! 🎉 Проект повинен працювати на http://localhost:3000 